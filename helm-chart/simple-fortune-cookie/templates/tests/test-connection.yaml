apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simple-fortune-cookie.fullname" . }}-test-connection"
  labels:
    {{- include "simple-fortune-cookie.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simple-fortune-cookie.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
